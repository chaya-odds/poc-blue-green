name: GREEN Image CI

on:
  push:
    branches: [ "cloud-develope" ]
  pull_request:
    branches: [ "cloud-develope" ]
    paths:
      - 'green.html'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
        echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
        IMAGE_TAG=green-${{ github.sha }}
        docker build . --file Dockerfile.green --tag ghcr.io/${{ github.repository }}:$IMAGE_TAG
        docker tag ghcr.io/${{ github.repository }}:$IMAGE_TAG ghcr.io/${{ github.repository }}:green-latest
        docker push ghcr.io/${{ github.repository }}:$IMAGE_TAG
        docker push ghcr.io/${{ github.repository }}:green-latest